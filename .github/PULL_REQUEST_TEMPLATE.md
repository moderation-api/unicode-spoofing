## What & why

<!-- What does this change and why? Link any related issue. -->

## Checklist

- [ ] Tests added/updated (including false-positive cases for multilingual text)
- [ ] `pnpm lint && pnpm check-types && pnpm test && pnpm build` pass locally
- [ ] Added a changeset (`pnpm changeset`) if this is a user-facing change
